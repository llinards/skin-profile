{% comment %}
  This section is used in the page template to render store pages like About us
  or Contact us.

  https://shopify.dev/docs/storefronts/themes/architecture/templates/page
{% endcomment %}

<script src="{{ 'policy-redirect.js' | asset_url }}" defer></script>

<div
  class="{% if section.settings.add_top_padding %}pt-24{% endif %}"
>
  {{ page.content }}
</div>

{% schema %}
{
  "name": "t:general.page",
  "settings": [
    {
      "type": "checkbox",
      "id": "add_top_padding",
      "label": "Ielikt atstarpi",
      "default": true,
      "info": "Ielikt atstarpi starp izvēlni un saturu."
    }
  ]
}
{% endschema %}
