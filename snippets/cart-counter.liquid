<div class="flex items-center justify-center z-2 w-1/7">
  <a href="{{ routes.cart_url }}">
    {% render 'icon-shopping-bag' %}
  </a>
  <p class="rounded-full ml-2 px-2" :class="open ? 'bg-black text-white' : 'bg-white text-black'">
    {{ cart.item_count }}
  </p>
</div>
