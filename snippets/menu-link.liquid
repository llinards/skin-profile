<a
  href="{{ link.url }}"
  class="lg:text-white text-base xl:text-lg py-1 border border-transparent hover:border-b-white transition-all duration-300 {% if link.active %}border-b-black lg:border-b-white{% endif %} {{ invert_class }}"
>
  {{- link.title -}}
</a>
